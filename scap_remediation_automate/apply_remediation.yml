---
- name: Apply SCAP Remediation
  hosts: all
  tasks:
    - name: Apply the remediation file from the master node
      ansible.builtin.command: ansible-playbook /home/master/remediations/{{ inventory_hostname }}-remediation.yml
